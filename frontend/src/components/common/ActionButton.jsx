/**
 * @deprecated Prefer `Button` from `./Button`.
 * Kept as a thin alias so existing imports keep working.
 */
export {
  default,
  AddButton,
  PrimaryButton,
  SecondaryButton,
  ViewButton,
  EditButton,
  SuccessButton,
  WarningButton,
  DangerButton,
  IconButton,
} from "./Button";
