import DocumentsLibrary from "./DocumentsLibrary";

/** Operator / tenant document library (v1 API — upload, versions, approval status). */
export default function DocumentsDashboard(props) {
  return <DocumentsLibrary {...props} />;
}
