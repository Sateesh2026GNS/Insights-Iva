import JobCardDetailsPage from "./JobCardDetailsPage";

/** Sales edit route wrapper — same unified job card UI in edit mode. */
export default function SalesJobCardPage() {
  return <JobCardDetailsPage initialMode="edit" />;
}
